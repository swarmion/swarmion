on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'packages/create-swarmion-app/**'
      - 'examples/**'
      - 'scripts/check-example.sh'

# cancel previous runs on the same PR
concurrency:
  group: ${{ github.ref }}/check-examples
  cancel-in-progress: true

name: 🔍 PR Check examples

env:
  CI: true
  NODE_VERSION: 16

defaults:
  run:
    shell: bash

jobs:
  list-examples:
    name: 🪣 List examples
    runs-on: ubuntu-latest
    outputs:
      examples: ${{ steps.list-example-folders.outputs.examples }}
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
      - id: list-example-folders
        run: echo "examples=$(ls examples | jq -R -s -c 'split("\n")[:-1]')" >> $GITHUB_OUTPUT
        shell: bash
  check-examples:
    name: 🔍 Check examples
    runs-on: ubuntu-latest
    needs: list-examples
    if: join(fromJson(needs.list-examples.outputs.examples)) != ''
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        example: ${{ fromJson(needs.list-examples.outputs.examples) }}
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          # We need to fetch all branches and commits so that Nx affected has a base to compare against.
          fetch-depth: 0
      - uses: nrwl/nx-set-shas@v3
      - name: Install & cache node dependencies
        uses: ./.github/actions/install-node-deps
      - name: '🔍 Check example'
        run: REF=${{ github.event.pull_request.head.sha }} EXAMPLE=${{matrix.example}} sh ./scripts/check-example.sh
        shell: bash
